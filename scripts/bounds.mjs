// Study-area bounds shared by the OSM preprocessing scripts.
// Must match src/world/geo/bounds.ts (FETCH area is slightly larger than the
// playable area so that streets/beach crossing the edges are complete).
export const FETCH_BOUNDS = { south: 38.965, north: 39.005, west: -74.84, east: -74.79 };
